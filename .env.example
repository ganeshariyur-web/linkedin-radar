# Set these as environment variables in Netlify (Site configuration → Environment variables).
# Never commit real values. Locally, export them in your shell before `npm run dev`.
TYPESAFE_API_KEY=
APIFY_TOKEN=
